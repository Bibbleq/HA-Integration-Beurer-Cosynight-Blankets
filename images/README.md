# Dashboard Card Screenshot

The file `dashboard-card-example.png` should contain a screenshot of the advanced dashboard card showing:

- Two blanket columns side-by-side (Ben's and Jen's blankets)
- Status header with active/off indicator (🔥 Active or ❄️ Off)
- Body zone selector with heat-level color coding (blue to red gradient)
- Feet zone selector with heat-level color coding
- Duration tile with +/- controls
- "Last refreshed" timestamp at the bottom

The problem statement referenced this image but the actual image data was not provided in the `<img>` tags.

**Action Required**: Replace the placeholder `dashboard-card-example.png` with the actual screenshot image.
